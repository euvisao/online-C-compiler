#include <stdio.h>
#include <stdlib.h>

int main() {

    int numero;
    printf("digite um número qualquer: ");
    scanf("%d", &numero);
    printf("o número digitado foi: %d", numero);
    
    return 0;
}
